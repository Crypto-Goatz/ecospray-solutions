// Re-export from new schema module for backwards compatibility
export { localBusinessSchema as getLocalBusinessSchema, faqSchema as getFAQSchema, breadcrumbSchema as getBreadcrumbSchema, serviceSchema as getServiceSchema, reviewSchema as getReviewSchema } from "./schema";
